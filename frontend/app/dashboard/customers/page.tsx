"use client";
import { useEffect,useState } from "react"; import { api } from "../../../lib/api";
export default function Customers(){const [items,setItems]=useState<any[]>([]);const [error,setError]=useState("");useEffect(()=>{api("/api/customers").then(setItems).catch(e=>setError(e.message))},[]);
return <main className="dashboard-content"><div className="panel-head"><div><div className="eyebrow">CRM</div><h1 style={{fontSize:32,margin:"8px 0"}}>Customers</h1><span>Your store's customer database.</span></div></div>{error&&<div className="error-box">{error}</div>}<div className="panel" style={{marginTop:20,overflowX:"auto"}}><table><thead><tr><th>Customer</th><th>Phone</th><th>Email</th><th>Orders</th><th>Spent</th></tr></thead><tbody>{items.map(c=><tr key={c._id}><td><b>{c.name}</b></td><td>{c.phone||"—"}</td><td>{c.email||"—"}</td><td>{c.totalOrders}</td><td>₹{Number(c.totalSpent||0).toLocaleString("en-IN")}</td></tr>)}{items.length===0&&<tr><td colSpan={5} style={{textAlign:"center",padding:40,color:"#999"}}>No customers yet.</td></tr>}</tbody></table></div></main>}
