"use client";
import{useEffect,useState}from"react";import{api}from"../../../lib/api";
export default function SEO(){const[form,setForm]=useState({seoTitle:"",seoDescription:"",faviconUrl:""}),[error,setError]=useState(""),[saved,setSaved]=useState(false);
useEffect(()=>{api("/api/store").then((s:any)=>setForm({seoTitle:s.seoTitle||s.name||"",seoDescription:s.seoDescription||"",faviconUrl:s.faviconUrl||""})).catch((e:any)=>setError(e.message))},[]);
async function save(e:any){e.preventDefault();setError("");try{await api("/api/store/seo",{method:"PUT",body:JSON.stringify(form)});setSaved(true)}catch(e:any){setError(e.message)}}
return <main className="dashboard-content"><div className="panel-head"><div><div className="eyebrow">MARKETING</div><h1 style={{fontSize:32,margin:"8px 0"}}>SEO & sharing</h1><span>Control search and browser metadata.</span></div></div>{error&&<div className="error-box">{error}</div>}{saved&&<div className="success-box">SEO settings saved.</div>}<form className="panel seo-form" onSubmit={save}><label>SEO title<input maxLength={70} value={form.seoTitle} onChange={e=>setForm({...form,seoTitle:e.target.value})}/></label><label>Meta description<textarea maxLength={160} value={form.seoDescription} onChange={e=>setForm({...form,seoDescription:e.target.value})}/></label><label>Favicon URL<input placeholder="https://..." value={form.faviconUrl} onChange={e=>setForm({...form,faviconUrl:e.target.value})}/></label><button className="button button-dark">Save SEO settings</button></form></main>}
