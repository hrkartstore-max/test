"use client";
import { useEffect,useState } from "react"; import { api } from "../../../lib/api";
const statuses=["pending","confirmed","processing","shipped","delivered","cancelled"];
export default function Orders(){const [orders,setOrders]=useState<any[]>([]);const [error,setError]=useState("");
async function load(){try{setOrders(await api("/api/orders"))}catch(e:any){setError(e.message)}} useEffect(()=>{load()},[]);
async function status(id:string,status:string){try{await api(`/api/orders/${id}/status`,{method:"PATCH",body:JSON.stringify({status})});load()}catch(e:any){setError(e.message)}}
return <main className="dashboard-content"><div className="panel-head"><div><div className="eyebrow">SALES</div><h1 style={{fontSize:32,margin:"8px 0"}}>Orders</h1><span>Track and update customer orders.</span></div></div>{error&&<div className="error-box">{error}</div>}<div className="panel" style={{marginTop:20,overflowX:"auto"}}><table><thead><tr><th>Order</th><th>Customer</th><th>Total</th><th>Payment</th><th>Status</th></tr></thead><tbody>{orders.map(o=><tr key={o._id}><td><b>{o.orderNumber}</b><div className="muted">{new Date(o.createdAt).toLocaleString()}</div></td><td>{o.customer?.name}<div className="muted">{o.customer?.phone}</div></td><td>₹{Number(o.total).toLocaleString("en-IN")}</td><td>{o.paymentStatus}</td><td><select value={o.status} onChange={e=>status(o._id,e.target.value)}>{statuses.map(s=><option key={s}>{s}</option>)}</select></td></tr>)}{orders.length===0&&<tr><td colSpan={5} style={{textAlign:"center",padding:40,color:"#999"}}>No orders yet.</td></tr>}</tbody></table></div></main>}
