"use client";
import{useEffect,useState}from"react";import{api}from"../../../lib/api";
export default function Audit(){const[items,setItems]=useState<any[]>([]),[error,setError]=useState("");useEffect(()=>{api("/api/audit-logs").then(setItems).catch(e=>setError(e.message))},[]);return <main className="dashboard-content"><div className="panel-head"><div><div className="eyebrow">SECURITY</div><h1 style={{fontSize:32,margin:"8px 0"}}>Audit logs</h1><span>Recent changes made in your store.</span></div></div>{error&&<div className="error-box">{error}</div>}<div className="panel" style={{marginTop:20,overflowX:"auto"}}><table><thead><tr><th>Time</th><th>Action</th><th>Entity</th><th>Details</th></tr></thead><tbody>{items.map(x=><tr key={x._id}><td>{new Date(x.createdAt).toLocaleString()}</td><td>{x.action}</td><td>{x.entity}</td><td><code>{JSON.stringify(x.meta||{})}</code></td></tr>)}</tbody></table></div></main>}
