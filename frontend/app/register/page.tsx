'use client';
import Link from 'next/link';import {Suspense,useState} from 'react';import {useSearchParams} from 'next/navigation';import {useRouter} from 'next/navigation';import {api} from '../../lib/api';
function RegisterContent(){const router=useRouter();const searchParams=useSearchParams();const selectedPlan=(searchParams.get('plan')||'free').toLowerCase();const[form,setForm]=useState({name:'',businessName:'',email:'',phone:'',password:''});const[confirm,setConfirm]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);const u=(k:string,v:string)=>setForm({...form,[k]:v});async function submit(e:React.FormEvent){e.preventDefault();if(form.password!==confirm)return setError('Passwords do not match.');setLoading(true);setError('');try{const response=await fetch('https://fdimdbtoeecewbgrskvk.supabase.co/functions/v1/hepra-register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await response.json().catch(()=>({}));if(!response.ok)throw new Error(d.message||`Registration failed (${response.status})`);localStorage.setItem('hepra_token',d.token);localStorage.setItem('hepra_selected_plan',selectedPlan);router.push('/onboarding')}catch(e:any){setError(e.message)}finally{setLoading(false)}}return <main className="auth-shell"><div className="auth-card wide"><Link href="/" className="brand"><span className="brand-mark">H</span> HEPRA</Link><h1>Create your store</h1><p>Set up your HEPRA business account.</p><div className="register-plan"><span>Selected plan</span><strong>{selectedPlan==='pro'?'🟣 PRO — ₹499/month':selectedPlan==='starter'?'🔵 STARTER — ₹299/month':'🟢 FREE — ₹0/month'}</strong></div>{error&&<div className="error-box">{error}</div>}<form className="form-grid" onSubmit={submit}><label>Full name<input required value={form.name} onChange={e=>u('name',e.target.value)}/></label><label>Business name<input required value={form.businessName} onChange={e=>u('businessName',e.target.value)}/></label><label>Email<input required type="email" value={form.email} onChange={e=>u('email',e.target.value)}/></label><label>Phone<input required value={form.phone} onChange={e=>u('phone',e.target.value)}/></label><label>Password<input required minLength={8} type="password" value={form.password} onChange={e=>u('password',e.target.value)}/></label><label>Confirm password<input required minLength={8} type="password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></label><button disabled={loading} className="button button-primary full span-2">{loading?'Creating your store…':'Create account →'}</button></form><p className="auth-foot">Already have an account? <Link href="/login">Sign in</Link></p></div></main>}


export default function Register(){
  return <Suspense fallback={<main className="auth-shell"><div className="auth-card wide">Loading registration…</div></main>}><RegisterContent /></Suspense>;
}
