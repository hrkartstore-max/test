# HEPRA Publish Flow

1. Create a page in `/dashboard/pages`.
2. Set the page status to `published`.
3. Open `/dashboard/publish`.
4. Click **Publish store**.
5. The public API serves published pages and published products.
6. Development preview: `/store/[host]`.
7. Production custom-domain routing still requires DNS + reverse-proxy/edge configuration.
